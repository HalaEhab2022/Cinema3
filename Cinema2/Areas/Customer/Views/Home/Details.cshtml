@model MovieWithRelatedVM

<!-- Product section-->
<section class="py-5">
    <div class="container px-4 px-lg-5 my-5">
        <div class="row gx-4 gx-lg-5 align-items-center">
            <div class="col-md-6"><img class="card-img-top mb-5 mb-md-0" src="~/images/@Model.Movie.MainImg" alt="..." /></div>
            <div class="col-md-6">
                <div class="small mb-1">Category Name: @Model.Movie.category.Name</div>
                <div class="small mb-1">Cinema Name: @Model.Movie.ciinema.Name</div>
                <div class="small mb-1">Date: @Model.Movie.DateTime</div>

                <h1 class="display-5 fw-bolder">@Model.Movie.Name</h1>
                <div class="fs-5 mb-5">
                    <span >@Model.Movie.TicketPrice.ToString("c")</span>
                   
                </div>
                <p class="lead">Lorem ipsum dolor sit amet consectetur adipisicing elit. Praesentium at dolorem quidem modi. Nam sequi consequatur obcaecati excepturi alias magni, accusamus eius blanditiis delectus ipsam minima ea iste laborum vero?</p>
                <div class="d-flex">

                    <form asp-action="AddToCart" asp-controller="Cart">
                        <input type="hidden" name="movieId" value="@Model.Movie.Id" />
                        <input class="form-control text-center me-3" id="inputQuantity" type="number" value="1" name="count" style="max-width: 5rem" />
                        <button class="btn btn-outline-dark flex-shrink-0 mt-3" type="submit">
                            <i class="bi-cart-fill me-1"></i>
                            Book
                        </button>
                    </form>

                </div>
            </div>
        </div>
    </div>
</section>


<!-- Related items section-->
<section class="py-5 bg-light">
    <div class="container px-4 px-lg-5 mt-5">
        <h2 class="fw-bolder mb-4">Related products</h2>
        <div class="row gx-4 gx-lg-5 row-cols-2 row-cols-md-3 row-cols-xl-4 justify-content-center">

            @foreach (var item in Model.RelatedMovies)
            {
                <div class="col mb-5">
                    <div class="card h-100">
                        <!-- Sale badge-->
                        @* <div class="badge bg-dark text-white position-absolute" style="top: 0.5rem; right: 0.5rem">Sale</div> *@
                        <!-- Product image-->
                        <img class="card-img-top" src="~/images/@item.MainImg" alt="..." />
                        <!-- Product details-->
                        <div class="card-body p-4">
                            <div class="text-center">
                                <!-- Product name-->
                                <h5 class="fw-bolder">@item.Name</h5>
                                
                                <!-- Product price-->
                                <span class="text-muted text-decoration-line-through">@item.TicketPrice.ToString("c")</span>
                               
                            </div>
                        </div>
                        <!-- Product actions-->
                        <div class="card-footer p-4 pt-0 border-top-0 bg-transparent">
                            <div class="text-center"><a class="btn btn-outline-dark mt-auto" href="#">Add to cart</a></div>
                        </div>
                    </div>
                </div>
            }

        </div>
    </div>
</section>




@* @model Movie
<div class="container">
    <div class="row mb-3 align-items-center my-3">
        <div class="col-md-6">
            <h2>Movie Details</h2>
        </div>
    </div>

    <div class="row mb-3 align-items-center my-3">
        <div class="col-md-10">
            <p style="font-size:30px; font-weight:600">Name : @Model.Name</p>
            <p style="font-size:30px; font-weight:600">Description : @Model.Description</p>
            <p style="font-size:30px; font-weight:600">Status : @Model.Status</p>
            <p style="font-size:30px; font-weight:600">TicketPrice : @Model.TicketPrice</p>
            <p style="font-size:30px; font-weight:600">Date : @Model.DateTime</p>
            <p style="font-size:30px; font-weight:600">Category : @Model.category.Name</p>
            <p style="font-size:30px; font-weight:600">Cinema : @Model.ciinema.Name</p>

            <div class="mb-3">
                <img id="preview" src="~/images/@Model.MainImg" alt="Current Image" style="max-width: 200px; border:1px solid #ccc; padding:5px;
                        border-radius:10px;" />
            </div>

            <div class="row">
                @if (Model.subImages != null && Model.subImages.Any())
                {
                    <div class="d-flex flex-wrap gap-3">
                        @foreach (var item in Model.subImages)
                        {
                             <div class="p-2" style="position: relative;">
                                <img src="~/images/movie-images/@item.Img"
                                     alt="Current Image"
                                     style="width:150px; height:110px; border:1px solid #ccc; padding:5px; border-radius:10px;" />
                            </div> 
                        }
                    </div>
                }
                
            </div> 
        </div>
    </div>
</div>




            *@